# Production pipeline (training + serving), pinned: the serving image must
# unpickle artifacts trained with these exact versions.
# (notebook extras live in requirements-dev.txt)
fastapi==0.139.0
imbalanced-learn==0.14.2
joblib==1.5.3
numpy==2.5.1
pandas==3.0.3
pydantic==2.13.4
scikit-learn==1.9.0
scipy==1.18.0
uvicorn==0.50.2
xgboost==3.3.0
